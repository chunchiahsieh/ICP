@using System.Text.Json
@{
    ViewData["Title"] = Localizer["Setting.DeliveryToList.Title"];
}

@section Styles {
    <link rel="stylesheet" href="~/lib/datatables/css/jquery.dataTables.min.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/lib/datatables/css/dataTables.bootstrap.min.css" asp-append-version="true" />
}

<div data-permissions="Views.Shared._SidebarNav.Setting.DeliveryToList" data-i18n-key="Views.Shared._SidebarNav.Setting.DeliveryToList">
    <div class="d-flex align-items-center justify-content-between mt-3 mb-3">
        <h4 class="mb-0">@Localizer["Setting.DeliveryToList.Title"]</h4>
        <div class="d-flex gap-2">
            <button type="button"
                    class="btn btn-outline-danger btn-sm"
                    id="btnBatchDelete"
                    data-permissions="Views.Setting.DeliveryToList.Delete"
                    data-i18n-key="Views.Setting.DeliveryToList.Delete">
                @Localizer["Common.BatchDelete"]
            </button>
            <button type="button"
                    class="btn btn-primary btn-sm"
                    id="btnCreate"
                    data-permissions="Views.Setting.DeliveryToList.Create"
                    data-i18n-key="Views.Setting.DeliveryToList.Create">
                @Localizer["Common.Add"]
            </button>
        </div>
    </div>
    <div id="pageMessage" class="alert alert-success py-2 d-none mb-3" role="alert"></div>
    <div id="DataDiv"></div>
</div>

<div class="d-none" aria-hidden="true">
    <span data-permissions="Views.Setting.DeliveryToList.View" data-i18n-key="Views.Setting.DeliveryToList.View">@Localizer["Setting.DeliveryToList.Title"]</span>
    <button type="button" data-permissions="Views.Setting.DeliveryToList.Edit" data-i18n-key="Views.Setting.DeliveryToList.Edit">@Localizer["Common.Edit"]</button>
    <button type="button" data-permissions="Views.Setting.DeliveryToList.Delete" data-i18n-key="Views.Setting.DeliveryToList.Delete">@Localizer["Common.Delete"]</button>
</div>

@await Html.PartialAsync("~/Views/Setting/DeliveryToList/View.EditModal.cshtml")
@await Html.PartialAsync("_CrudConfirmModal")

@section Scripts {
    <script src="~/lib/datatables/js/jquery.dataTables.min.js" asp-append-version="true"></script>
    <script src="~/lib/datatables/js/dataTables.bootstrap.min.js" asp-append-version="true"></script>
    <script src="~/js/pro-datatables.js" asp-append-version="true"></script>
    <script src="~/js/pro-system-config.js" asp-append-version="true"></script>
    <script>
        $(function () {
            var filterFieldMap = {
                'filter-Key1': 'Key1s',
                'filter-Value1': 'Value1s',
                'filter-Value2': 'Value2s',
                'filter-Value3': 'Value3s',
                'filter-Value4': 'Value4s',
                'filter-Value5': 'Value5s',
                'filter-Value6': 'Value6s'
            };

            var tableInstance;
            if (window.ProDataTables && ProDataTables.initUsers) {
                tableInstance = ProDataTables.initUsers(ProDataTables.buildConfig({
                    filterFieldMap: filterFieldMap,
                    filterOptionsUrl: '@Url.Action("GetFilterOptions", "DeliveryToList")',
                    queryUrl: '@Url.Action("Query", "DeliveryToList")',
                    initialSort: [[2, 'asc']]
                }));
            }

            if (window.ProSystemConfig) {
                ProSystemConfig.init({
                    category: 'DeliveryToList',
                    getUrl: '@Url.Action("Get", "DeliveryToList")',
                    saveUrl: '@Url.Action("Save", "DeliveryToList")',
                    deleteUrl: '@Url.Action("Delete", "DeliveryToList")',
                    batchDeleteUrl: '@Url.Action("BatchDelete", "DeliveryToList")',
                    batchDeleteConfirmMessage: @Html.Raw(JsonSerializer.Serialize(Localizer["Setting.SystemConfig.BatchDeleteConfirm"].Value)),
                    createTitle: @Html.Raw(JsonSerializer.Serialize(Localizer["Setting.SystemConfig.CreateTitle"].Value)),
                    editTitle: @Html.Raw(JsonSerializer.Serialize(Localizer["Setting.SystemConfig.EditTitle"].Value)),
                    deleteConfirmMessage: @Html.Raw(JsonSerializer.Serialize(Localizer["Setting.SystemConfig.DeleteConfirm"].Value)),
                    onSuccess: function () {
                        if (tableInstance && tableInstance.reload) {
                            tableInstance.reload();
                        }
                    }
                });
            }
        });
    </script>
}
