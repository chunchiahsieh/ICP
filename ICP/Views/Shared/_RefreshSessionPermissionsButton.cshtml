<button type="button"
        class="@(ViewData["RefreshPermissionsButtonClass"] ?? "btn btn-outline-secondary btn-sm")"
        data-icp-refresh-permissions="true">
    @Localizer["Auth.RefreshSessionPermissions"]
</button>
